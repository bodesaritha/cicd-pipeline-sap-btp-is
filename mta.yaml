_schema-version: '3.2'
ID: TESTPACKAGE
version: 0.0.1

modules:
  - name: IFLOW-1 # The name of your iFlow artifact
    type: com.sap.integration
    path: IFLOW-1
    requires:
      - name: process-api-instance # This name must match the resource name below
        parameters:
          content-target: true

resources:
  - name: process-api-instance # This name must match the 'requires' name above
    type: org.cloudfoundry.managed-service
    parameters:
      service: it-rt # The CF service for Cloud Integration
      service-plan: api # The specific service plan for content deployment
