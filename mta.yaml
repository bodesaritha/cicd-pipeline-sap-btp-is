---
_schema-version: "3.2"
ID: my-iflow-deployment-project-new
version: 0.0.1
modules:
- name: deploy-iflow-artifact
  type: com.sap.integration
  path: TEST-1.zip
  requires:
  - name: is-api-instance
    parameters:
      content-target: true
      # ADD THIS CONFIGURATION BLOCK:
      config:
        module:
          name: TEST-1 # This should be the name of your integration flow/content package in CPI
          type: com.sap.integration # The type of the module (e.g., Integration Flow, ContentPackage)
          parameters:
            resource-type: IntegrationFlow # Or ContentPackage, depending on what IFLOW-1 represents in CPI
            technical-name: TEST-1 # The technical name of the resource in CPI
            # If IFLOW-1 is an Integration Flow and you want to specify its ID, you might add:
            # resource-id: <actual_CPI_integration_flow_ID>
resources:
- name: is-api-instance
  type: org.cloudfoundry.existing-service
  parameters:
    service: it-rt
    service-plan: api
