_schema-version: "3.2"
ID: "TESTPACKAGE"
version: "0.0.1"
description: "This is the test package and iflow"
modules:
  - name: "TESTPACKAGE"
    type: com.sap.integration
    path: "IntegrationArtifacts/IFLOW-1.zip" # This is the folder containing your CPI flow artifacts
    parameters:
      content-target: true
